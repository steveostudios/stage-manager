header#header
  ul.nav
    li 
      a(href='/events') StageManager
  ul.nav.pull-right
    if (req.isAuthenticated())
      li
        if (req.session.isAdmin)
          a(href="/users/"+req.user.id, title="Profile")= req.user.org
        else
          = req.user.org
      li
        a(href="/logout", title="logout") Logout
    else
      li
        a(href="/login", title="Login") Login
